# 🔐 Система логинов и ролей

## Разделение ролей

Система имеет **два типа пользователей** с разными логинами:

### 👤 Клиенты (Customers)

- **Регистрация:** Только через Telegram бота (`/start`)
- **Логин:** Автоматически генерируется в формате `user_<telegram_id>` (например: `user_123456789`)
- **Пароль:** Автоматически генерируется случайный пароль (12 символов)
- **Роль:** `customer`
- **Доступ:** Каталог товаров, корзина, заказы
- **Пример логина:** `user_123456789`

**Как получить логин клиента:**
1. Откройте Telegram бота
2. Отправьте команду `/start`
3. Пройдите регистрацию (имя, телефон)
4. Бот отправит вам логин и пароль
5. Используйте эти данные для входа в веб-приложение

### 👨‍💼 Администраторы (Admins)

- **Регистрация:** Создаётся автоматически при миграции базы данных
- **Логин:** Настраивается через переменную окружения `ADMIN_USERNAME` (по умолчанию: `admin`)
- **Пароль:** Настраивается через переменную окружения `ADMIN_PASSWORD` (по умолчанию: `admin123`)
- **Роль:** `admin`
- **Доступ:** Админ-панель, управление товарами, заказами, категориями
- **Пример логина:** `admin` или `Davron` (если установлено в Railway)

**Как получить логин администратора:**
1. Логин администратора создаётся автоматически при первом запуске
2. По умолчанию: `admin` / `admin123`
3. Можно изменить через переменные окружения в Railway:
   - `ADMIN_USERNAME` - логин администратора
   - `ADMIN_PASSWORD` - пароль администратора

## 🔒 Безопасность

- Клиенты **не могут** зарегистрироваться с логином `admin` или `admin_*`
- Администраторы **не могут** быть созданы через Telegram бота
- Каждый тип пользователя имеет свой отдельный логин
- Пароли хранятся в зашифрованном виде (bcrypt)

## 📝 Примеры

### Клиент:
```
Логин: user_123456789
Пароль: a1b2c3d4e5f6
Роль: customer
```

### Администратор:
```
Логин: admin (или Davron)
Пароль: admin123 (или ваш пароль из Railway)
Роль: admin
```

## ⚙️ Настройка в Railway

Для изменения логина/пароля администратора:

1. Откройте ваш проект в Railway
2. Перейдите в **Variables**
3. Добавьте/измените:
   - `ADMIN_USERNAME` = `ваш_логин`
   - `ADMIN_PASSWORD` = `ваш_пароль`
4. Перезапустите сервис

**Важно:** После изменения переменных окружения нужно перезапустить сервис, чтобы изменения вступили в силу.

## 🚨 Важные замечания

1. **Не используйте один логин для админа и клиента!**
2. Клиенты регистрируются только через бота
3. Админы создаются только через миграцию базы данных
4. Если забыли пароль клиента - нужно обратиться к администратору или зарегистрироваться заново через бота



