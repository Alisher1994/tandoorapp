# 📝 Пошаговая инструкция - что делать сейчас

## 🎯 Шаг 1: Подготовка локального окружения

### 1.1 Установите необходимые инструменты

```bash
# Проверьте, что установлены:
node --version  # Должно быть v18 или выше
npm --version   # Должно быть v9 или выше
git --version   # Для работы с GitHub
```

Если чего-то нет - установите:
- Node.js: https://nodejs.org/
- Git: https://git-scm.com/

### 1.2 Установите PostgreSQL локально

**Вариант A: Docker (рекомендуется)**
```bash
docker run -d --name postgres-orders \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=orders_db \
  -p 5432:5432 \
  postgres:15
```

**Вариант B: Установка напрямую**
- Windows: https://www.postgresql.org/download/windows/
- Mac: `brew install postgresql`
- Linux: `sudo apt-get install postgresql`

### 1.3 Установите зависимости проекта

```bash
# В корне проекта
npm install

# В папке client
cd client
npm install
cd ..
```

---

## 🤖 Шаг 2: Создание Telegram бота

### 2.1 Создайте бота через BotFather

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду: `/newbot`
4. Введите имя бота (например: "Мой магазин")
5. Введите username бота (должен заканчиваться на `bot`, например: `my_shop_bot`)
6. **СОХРАНИТЕ ТОКЕН** - он понадобится позже

### 2.2 Создайте группу для админов

1. Создайте новую группу в Telegram
2. Добавьте вашего бота в группу (как администратора)
3. Отправьте любое сообщение в группу
4. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
5. Найдите `"chat":{"id":-1001234567890}` - это ID группы (отрицательное число)
6. **СОХРАНИТЕ ЭТОТ ID**

---

## ⚙️ Шаг 3: Настройка переменных окружения

### 3.1 Создайте файл .env в корне проекта

```bash
# Скопируйте env.example в .env
cp env.example .env
```

### 3.2 Отредактируйте .env файл

Откройте `.env` и заполните:

```env
# Server
PORT=3000
NODE_ENV=development

# Database (для локальной разработки)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/orders_db

# JWT - придумайте сложный ключ (минимум 32 символа)
JWT_SECRET=my-super-secret-jwt-key-change-this-in-production-12345
JWT_EXPIRES_IN=7d

# Telegram Bot - вставьте токен из шага 2.1
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram Admin Chat ID - вставьте ID из шага 2.2
TELEGRAM_ADMIN_CHAT_ID=-1001234567890

# Для локальной разработки
TELEGRAM_WEB_APP_URL=http://localhost:3001
FRONTEND_URL=http://localhost:3001

# Admin credentials (для первого админа)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123
```

---

## 🗄️ Шаг 4: Настройка базы данных

### 4.1 Создайте базу данных

```bash
# Если используете Docker
docker exec -it postgres-orders psql -U postgres

# Или подключитесь к PostgreSQL напрямую
psql -U postgres
```

В psql выполните:
```sql
CREATE DATABASE orders_db;
\q
```

### 4.2 Запустите миграции

```bash
# В корне проекта
npm run migrate
```

Должно появиться:
```
✅ Database schema created
✅ Default admin user created: admin / admin123
✅ Migration completed successfully
```

---

## 🚀 Шаг 5: Запуск приложения

### 5.1 Запустите в режиме разработки

```bash
# В корне проекта
npm run dev
```

Это запустит:
- Backend на http://localhost:3000
- Frontend на http://localhost:3001

### 5.2 Проверьте, что всё работает

1. Откройте http://localhost:3001
2. Должна открыться страница входа
3. Войдите с данными: `admin` / `admin123`
4. Должна открыться админ-панель

---

## 🧪 Шаг 6: Тестирование системы

### 6.1 Тест Telegram бота

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Введите имя (например: "Иван Иванов")
4. Введите телефон (например: "+998901234567")
5. Получите логин и пароль
6. **СОХРАНИТЕ ИХ!**

### 6.2 Тест веб-приложения

1. Откройте http://localhost:3001
2. Войдите с полученными логином и паролем
3. Должен открыться каталог (пока пустой)

### 6.3 Тест админ-панели

1. Войдите как админ: `admin` / `admin123`
2. Перейдите на вкладку "Товары"
3. Попробуйте добавить товар (пока кнопка есть, но функционал нужно доработать)

---

## 📦 Шаг 7: Добавление тестовых данных

### 7.1 Добавьте категории через SQL

```bash
psql -U postgres -d orders_db
```

```sql
INSERT INTO categories (name_ru, name_uz, image_url, sort_order) VALUES
('Овощи', 'Sabzavotlar', 'https://via.placeholder.com/150', 1),
('Фрукты', 'Mevalar', 'https://via.placeholder.com/150', 2),
('Мясо', 'Go''sht', 'https://via.placeholder.com/150', 3);

\q
```

### 7.2 Добавьте товары через SQL

```bash
psql -U postgres -d orders_db
```

```sql
-- Получите ID категорий
SELECT id, name_ru FROM categories;

-- Добавьте товары (замените category_id на реальные ID)
INSERT INTO products (category_id, name_ru, name_uz, price, unit, in_stock) VALUES
(1, 'Помидоры', 'Pomidor', 15000, 'кг', true),
(1, 'Огурцы', 'Bodring', 12000, 'кг', true),
(2, 'Яблоки', 'Olma', 20000, 'кг', true),
(2, 'Бананы', 'Banan', 25000, 'кг', true),
(3, 'Говядина', 'Mol go''shti', 80000, 'кг', true);

\q
```

---

## ✅ Шаг 8: Проверка полного цикла

### 8.1 Создайте заказ как клиент

1. Войдите как клиент (через Telegram бота)
2. Откройте каталог
3. Добавьте товары в корзину
4. Оформите заказ
5. Проверьте, что заказ появился в "Мои заказы"

### 8.2 Обработайте заказ как админ

1. Войдите как админ
2. Откройте админ-панель
3. Найдите новый заказ
4. Измените статус на "Готовится"
5. Проверьте, что клиент получил уведомление в Telegram

---

## 🚢 Шаг 9: Подготовка к деплою на Railway

### 9.1 Создайте GitHub репозиторий

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Order management system"

# Создайте репозиторий на GitHub и подключите
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

### 9.2 Подготовьте Railway

1. Зарегистрируйтесь на https://railway.app
2. Подключите GitHub аккаунт
3. Создайте новый проект
4. Выберите "Deploy from GitHub repo"
5. Выберите ваш репозиторий

---

## 📋 Чек-лист готовности

Отметьте выполненные пункты:

- [ ] Node.js установлен
- [ ] PostgreSQL установлен и запущен
- [ ] Зависимости установлены (`npm install`)
- [ ] Telegram бот создан
- [ ] Группа для админов создана
- [ ] Файл `.env` создан и заполнен
- [ ] База данных создана
- [ ] Миграции выполнены
- [ ] Приложение запускается локально
- [ ] Бот отвечает на `/start`
- [ ] Можно войти в веб-приложение
- [ ] Админ-панель работает
- [ ] Тестовые данные добавлены
- [ ] Полный цикл заказа протестирован
- [ ] GitHub репозиторий создан
- [ ] Railway проект создан

---

## 🆘 Если что-то не работает

### Проблема: Бот не отвечает
- Проверьте `TELEGRAM_BOT_TOKEN` в `.env`
- Убедитесь, что сервер запущен
- Проверьте логи: `npm run dev`

### Проблема: Ошибка подключения к БД
- Проверьте, что PostgreSQL запущен
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что база данных создана

### Проблема: Страница не открывается
- Проверьте, что оба сервера запущены (backend и frontend)
- Откройте http://localhost:3001
- Проверьте консоль браузера на ошибки

### Проблема: Миграции не работают
- Убедитесь, что база данных создана
- Проверьте права доступа к PostgreSQL
- Попробуйте выполнить SQL вручную из `server/database/schema.sql`

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Работающая система локально
- ✅ Telegram бот для регистрации
- ✅ Веб-приложение для клиентов
- ✅ Админ-панель
- ✅ Готовность к деплою на Railway

**Начните с Шага 1 и двигайтесь последовательно!**

