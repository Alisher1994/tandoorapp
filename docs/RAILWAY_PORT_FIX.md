# 🔧 Исправление проблемы с портом

## Проблема
Railway показывает порт 8080 в настройках Networking, но приложение может слушать на другом порту.

## Решение

### Вариант 1: Изменить порт в Railway (рекомендуется)

1. В Railway откройте сервис **"tandoorapp"**
2. Перейдите на вкладку **"Settings"**
3. Найдите раздел **"Networking"** → **"Public Networking"**
4. Если там указан порт 8080, измените его на **3000** (или удалите и создайте заново)
5. Railway автоматически перезапустит сервис

### Вариант 2: Использовать порт, который задает Railway

Railway автоматически устанавливает переменную `PORT`. Приложение уже использует её:
```javascript
const PORT = process.env.PORT || 3000;
```

**Проверьте в Railway:**
1. Сервис "tandoorapp" → Variables
2. Найдите переменную `PORT`
3. Если её нет или она равна 8080, измените на `3000`

### Вариант 3: Настроить порт в Railway Networking

1. В Railway откройте сервис **"tandoorapp"**
2. Перейдите на вкладку **"Settings"** → **"Networking"**
3. В разделе **"Public Networking"** нажмите на иконку редактирования (карандаш)
4. Измените порт с 8080 на **3000**
5. Сохраните изменения

## Проверка

После исправления в логах должно быть:
```
📌 PORT from environment: 3000
🚀 Server running on port 3000
🌐 Listening on 0.0.0.0:3000
```

## Важно

- Railway автоматически проксирует HTTP трафик на порт, указанный в переменной `PORT`
- Если в Networking указан другой порт, Railway не сможет подключиться к приложению
- Убедитесь, что порт в Networking совпадает с портом в переменной `PORT`




